# Token Hotspot 记录：issue #221 派生版本机制阶段 A 评估 + 落地 + 合并

> 日期：2026-07-16
> 任务类型：模板维护（快速续接 → 提案重新评估 → 阶段 A 实现 + PR + 合并）
> 触发条件：`ai/session-rules.md` §4.1（从快速续接进入分析 / 写入后完整读取规则回退包 + 多次 check-template + 跨 AI 结论交叉核对）

## 热点工作

- 完整规则回退包读取：`rules-core` / `global-rules` / `document-lifecycle-rules`（~547 行，本轮最大单文件）/ `implementation-lifecycle-rules` / `session-rules` / `commands/README` / `project-rules` + `MAINTAINERS.md` / `CONTRIBUTING.md` / `template-sync.json`。双重触发：续接记录与 Git 事实冲突 + 模板维护（`rules-core` §3 要求完整回退包）。
- 大文件：`scripts/new-project.sh` 全文（332 行，为定位烟测失败根因 `git archive HEAD` 机制）、`global-rules.md`（~187 行）、`_proposals/TEMPLATE-UPGRADE-token-hotspot-records.md`（254 行，确认 hotspot 机制落地点）。
- `check-template.sh` 跑 2 次（提交前失败 1843 行 + 提交后通过 1843 行），均用重定向 + `tail` / `grep` 摘要，未全刷入对话。
- 跨 AI 结论交叉核对：对照 Codex CLI 的 PR A/B/C 评估记录与本轮 Claude 独立评估，收敛拆分。
- 额外读取：`e2e-sync-check.sh` / `e2e-regression-checklist.md` / `e2e-report-template.md` / `.github/pull_request_template.md` / 一个现有 TEMPLATE-UPGRADE 样例 + 一份现有 hotspot 记录（对齐格式）。

## 热点判断

- **主要成本**：完整规则回退包，尤其 `document-lifecycle-rules.md`（547 行）。
- **次要**：`new-project.sh` 全文（必要——定位 `--local` 用 `git archive HEAD` 不含未提交改动的根因）；`check-template.sh` 2 次（必要诊断：提交前失败→定位→提交后验证）。
- **总量级**：medium-high。

## 质量影响

- 完整规则读取带来多处关键合规发现，均为高价值：MINOR 定级 → 需 L3 `e2e-sync-check`（MAINTAINERS §3）；issue 镜像是 raw 副本不能改 → 新建 TEMPLATE-UPGRADE；`template-sync.json` 无需改；README「当前版本」是 VERSION 指针无需改；PR body 须避免 close 关键词误关 #221。
- H-001（same-session rule-reuse）生效：同会话只读一次完整回退包，后续 commit / push / PR / 合并未重读规则。
- H-003（验证证据摘要）生效：`check-template` 成功输出只留 exit code + 尾行结论，未带 1843 行全量断言。

## 优化建议

- **候选 H-00x（任务路由细化）**：模板维护任务路由下，`document-lifecycle-rules.md`（文档生成 / 追溯链）与本轮纯脚本 / 种子 / 断言维护关联度低，可考虑按需读相关章节而非完整回退包必读全文。与 `rules-core` §3「不确定读完整回退包」存在张力，需评估是否为「纯脚本 / 自检维护」子类降级 document-lifecycle 为按需读取。
- **check-template 烟测失败精确定位**：失败时可输出「断言名 + 实际文件内容 vs 期望模式」，减少 AI 手动 `grep` 1843 行日志 + 读脚本定位根因的成本（本次靠手动 grep + 读 new-project.sh 定位 `archive HEAD` 机制）。
- **H-002（multi-AI claim→evidence 表）仍候选未落地**：本轮交叉核对 Codex 评估时，若对方提供「结论→证据 file:line」表可减少重读。本轮因 Git diff 已在手，成本可控，但属可推广模式。

## 关联

- issue #221；PR #223（squash 合并 commit `5a17f39`）；分支 `feat/new-project-version-defaults`（已合并删除）；当前 main = v1.53.0。
- 提案：`_proposals/TEMPLATE-UPGRADE-derived-version-mechanism.md`（阶段 A 已合并，B/C 待续）。
- 机制：`_proposals/TEMPLATE-UPGRADE-token-hotspot-records.md`（H-001 / H-003 已落地，H-002 候选）。

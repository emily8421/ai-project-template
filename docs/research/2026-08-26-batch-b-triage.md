# Batch B 立项评估：目录治理全景（#370 + #374 + 点目录治理提案）

> 撰写：2026-08-26，模板维护者会话（用户点单「Batch B 立项」）
> 类型：C1 提案 triage 续轮（前轮 = `2026-08-26-c1-proposal-triage.md`，Batch A → v1.68.0 已闭环）
> 定位：本报告只做立项评估与分批计划，不是实施授权；规则改动须用户逐项拍板后另开实施 PR。

## 0. 顶部锚定

- 模板基线：main @ `db58113`，VERSION v1.68.0，工作区干净。
- 远端实查（2026-08-26 本轮）：open issue 4 个——#370 / #374 / #334 / #290（前轮的 #386/#390/#392 已随 Batch A 关闭，open 集合缩小）。
- 本地收件箱：4 份提案（3 份部分落地维持 Gate + 1 份本轮新增点目录治理）。
- 镜像新鲜度：#290/#334/#370/#374 四份镜像均 2026-08-21 落，远端 updatedAt 未变（本轮实查核对），**fresh**。

## 1. 立项范围（三份提案构成「目录治理全景」）

| 提案 | 粒度 | 回答的问题 | 落点 |
|---|---|---|---|
| #370 目录划分依据 | 目录级 | 放哪个**目录**、凭什么 | `global-rules §5` + `web-fullstack-profile §4` |
| #374 文件治理三层体系 | 文件级 | 放哪个**文件**、文件里放什么 | `web-fullstack-profile §5/§5.1` + `global-rules §2` 可选指针 |
| 点目录治理（PR #400 新入箱） | 根目录级 | 根目录谁能建什么、点/下划线/无前缀怎么分 | `global-rules §5` + `post-sync-cleanup` + `.gitignore` + `beginner-guide` |

三者判断口径同源（「这行代码/这个文件因为什么而变？」），正交不重叠，LUMEN 单项目但三个不同实证场景（逐目录评审 / 根因分析 / 点目录增殖），建议聚合为 **Batch B → v1.69.0（MINOR）**。

## 2. 三份提案现状核对（本轮实查）

- **#370**：目标落点现状吻合——`global-rules §5` 三层区表后可直接补五条依据；`profile §4` 目录树 + 四问已存在（四问即依据 #2-#5 的提问式变体，提案自述升级关系成立）。方案 A（并采）可行。
- **#374**：`profile §5` 阈值表 + `§5.1` 主应用职责边界现状与提案假设完全吻合；L1 职责表 = §5.1 模式推广、L3 = 表头措辞修正、L2 = 新增机制描述节。LUMEN 三周实证（v3.12.5/v3.13.0 CI 9/9 绿）。
- **点目录治理**：提案为前轮会话产物（PR #400），五项拟改，落点已核对存在。

## 3. 依赖与冲突

- **#374 × #334（弱依赖，沿前轮 Q-C 拍板）**：#374 L2 结构检查项以 #334 FastAPI Adapter 条目为规范源**部分吸收**（只吸收「业务层不 import web 框架」一条作 L2 示例的规范源引用，不建 Adapter 载体）；#334 本体维持暂缓（仍单项目实证）。
- **#370 × 点目录治理**：同触 `global-rules §5` 但节不同（目录依据 vs 命名空间准入），无正文冲突；实施时注意章节插入顺序。
- **#374 × #370**：自声明两级粒度，判断口径句呼应即可，无冲突。
- 三者与 Batch A 已落地内容（root-reorg checklist / 指针式元信息 / 跨仓角色声明）零交集。

## 4. 分批与 PR 拆分建议

**Batch B = 3 个内容 PR + 1 个版本收口 PR + 1 个归档 PR**（沿用 Batch A 惯例）：

| PR | 内容 | 版本动作 |
|---|---|---|
| B1 | #370：`global-rules §5` 补五条目录划分依据（通用层）+ `profile §4` 补依据→目录树映射说明（Web 特化） | — |
| B2 | #374：`profile §5` 表头措辞改「症状信号」+ §5.1 升级为通用职责表模式（新增「文件职责表」节）+ 新增 L2 结构检查机制节（含 #334 条目吸收）+ `global-rules §2` L0 一行指针 | — |
| B3 | 点目录治理：命名空间三分 + 准入清单 + `.ai/` 白名单 + post-sync-cleanup 双文件审计项 + `.gitignore` 补 `.tmp/` + beginner-guide 补说明 | — |
| B4 | 版本收口：VERSION → v1.69.0 + CHANGELOG 双文件 Batch B 全量条目 | v1.69.0 |
| B5 | 归档：三份提案镜像移入 `_governance/_archive/`，远端 #370/#374 关闭 | — |

拆 3 内容 PR 的理由：三份提案落点文件部分重叠（`global-rules §5` 被 B1/B3 都触碰、`profile` 被 B1/B2 都触碰），分 PR 便于 CI 定位与回滚；B4 收口避免中间版本号漂移。

**6 仓同步搭车**：v1.69.0 发版后启动 6 仓同步窗口（v1.62.0/v1.64.0/v1.66.0/v1.67.x → v1.69.0），兑现「聚合一次发」拍板。LumiOne/gmbl not-local 不动。

## 5. 不纳入本轮

- #334 本体（Adapter 载体）：维持暂缓，仅 B2 吸收一条规范源引用。
- #290：维持 DEFER。
- 本地 3 份提案：各自 Gate 未变（Batch 4 待真实派生 ≥1 / Batch 3-5 缓议 / P2.1-P2.2 继续观察）。

## 6. 风险

- **R1**：B1/B3 同触 `global-rules §5`，顺序实施（B1 先 B3 后）避免 rebase 冲突；B3 实施前 rebase main。
- **R2**：#374 职责表是「模式骨架」非强制规范，措辞须保持「派生项目按栈填充」口径，防止变成框架绑定（提案 §2.1 已注意）。
- **R3**：v1.69.0 为 MINOR，发布前按惯例跑 check-template 全量 + md-clean 全范围。

## 7. 待拍板问题

| ID | 问题 | AI 建议 | 建议依据 | 备选 | 取舍 |
|---|---|---|---|---|---|
| Q-A | Batch B 范围 = 三提案聚合 v1.69.0？ | **是**（3 内容 PR + 收口 + 归档） | 口径同源、落点重叠、一次 triage 成本最低 | 拆两批（#370+#374 先、点目录后） | 拆批多一轮 triage + 两轮发版同步成本，无实质风险差异 |
| Q-B | B2 吸收 #334 FastAPI 条目（沿 Q-C 拍板）确认执行？ | **是**，仅作 L2 规范源引用 | 前轮 Q-C 已拍板方向；不建 Adapter 载体 | 不吸收（L2 示例只用 profile 既有口径） | 不吸收则 L2 示例规范源弱一档，#334 继续躺平无消费 |
| Q-C | v1.69.0 后 6 仓同步窗口立即启动？ | **是**（本机 5 仓，not-local 2 仓不动） | 兑现「聚合一次发」；v1.68.0+v1.69.0 一起带 | 再等一轮 | 再等则落后版本继续扩大 |
| Q-D | B1-B3 是否各跑 smoke（new-project 三形态）？ | **是**（B4 收口前统一跑一次即可，不必每 PR） | 落点全是规则/文档文件，无脚本改动；三形态 smoke 一次覆盖 | 每 PR 跑 | 每 PR 跑重复成本高，收益低 |

# 05 技术方案

## 1. 技术栈与版本
- Python 3.11
- 标准库 `re`

## 2. 关键技术决策
- 纯库接口而非命令或服务：需求只要求代码复用
- 纯函数实现：便于测试和复用，不引入环境依赖
- 无持久化：处理结果直接返回调用方，不保存状态

## 3. Phase 技术约束
与 `ai/project-rules.md` §1、§2 一致：当前 Phase 允许内部库、同步字符串处理、标准库实现；禁止 CLI、HTTP API、数据库和第三方框架。

## 4. 编码约定
详见 `ai/project-rules.md` §5，本文不重复。

# TEMPLATE-UPGRADE: document-lifecycle-rules §5 子节重复编号修正（§5.4 ×2）

> 来源：模板维护者（2026-09-09 C1 triage 顺带发现；编号连续性属既有「章节编号是稳定锚点」条款管辖）
> 状态：已落地（PR #446，squash `4601c42`，v1.73.0）；2026-09-09 归档
> 目标版本：v1.73.0（随 LUMEN 回流批聚合发布，不单独触发版本递增）
> Release impact：patch 级内容并入 MINOR 发布（编号修正，不改任何规则语义）
> Release strategy：同主题聚合（Batch B 窗口内独立微 PR）

## 1. 动机（去项目化）

`ai/document-lifecycle-rules.md` §5 存在两个 `### 5.4` 子节：「5.4 UI 原型策略触发与边界规则」与「5.4 04-05 总体设计风险验证规则」，其后为「5.5 06-07 DB / API 契约状态与升阶段门槛」。章节编号是全模板的稳定锚点（路由表章节级标注、命令文件章节级引用都依赖它），重复编号使「§5.4」指向不唯一，违反编号连续性。

## 1.1 与既有规则的关系（去重）

- **v1.60.1 全模板重编号**：**同类问题的存量残留**——该轮重编号未覆盖本处（呼应「自检连续性盲区」教训：check-template 只查 project-rules §2.x 连续性，未覆盖本文件 §5.x）。
- **`ai/global-rules.md` §5「文档编号规范」**：**执行对象**——本修正是该规范的落地补漏，不新增机制。
- **#441（路由粒度分层，Batch B-1 已落地）**：**依赖方向相反**——#441 的章节级标注依赖编号唯一性，本修正消除其现实障碍，互为支撑。

**本提案不重复它们**：只修一处存量编号缺陷，不扩自检断言（§5.x 编号连续性暂不引入 CI 检查，避免过度治理；如未来再发同类问题可再评估）。

## 2. 拟改

- `### 5.4 04-05 总体设计风险验证规则` → `### 5.5 04-05 总体设计风险验证规则`
- `### 5.5 06-07 DB / API 契约状态与升阶段门槛` → `### 5.6 06-07 DB / API 契约状态与升阶段门槛`
- 修正后 §5 子节序列：5.1 / 5.2 / 5.2.1 / 5.3 / 5.4 / 5.5 / 5.6，无重复无断档。

## 3. 版本影响

无独立版本动作（patch 级内容，随 v1.73.0 MINOR 聚合发布）。

## 4. 影响面

- 修改：`ai/document-lifecycle-rules.md` ×1（两处标题编号）。
- 波及面已核查（2026-09-09 全仓 grep）：两个旧编号（§5.4 / §5.5）在 `ai/`、`scripts/`、`template-docs/`、根文档与本文件内部均**无交叉引用**（其余命中均为 git-guide、docs/research 等文件自身编号体系）。

## 5. 验证方式

- `git diff --check` + `bash scripts/check-template.sh --summary` 全量自检 + 人工核对修正后 §5 子节序列。
- PR CI（Template Check）通过后合并。

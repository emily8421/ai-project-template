# L3 端到端回归报告

> Template-only notice: 本文件由 ai-project-template 模板维护者维护，仅存在于模板仓（v1.66.0 起不进入下行同步清单）；历史版本曾下行，派生项目中的残留可安全删除。

> 模板发布前 L3 回归记录（v1.73.0 MINOR 发版门）。

## 基本信息

- 待发布版本：v1.73.0（Batch B 聚合发版：#441 路由粒度分层 / #440 任务卡执行记录 / #439 阶段准出判据基准 / §5.4 重复编号修正，PR #443–#446）
- 测试日期：2026-09-09
- 测试人：AI 维护者会话（结果待维护者确认）
- 环境（OS / Git Bash / PowerShell / `gh`）：Windows 11 / Git Bash（MSYS）+ PowerShell 5.1 / gh（emily8421）
- 测试派生项目：临时派生 fixture（非 `ai-project-template-e2e` 专用仓）；doc-standards 镜像检查使用 check-template 内建临时工程

## 自动化项（`bash scripts/e2e-sync-check.sh`）

- check-template（含同步链路 + doc-standards 镜像 + 新项目烟测）：✅
- sync-all-derived 批量烟测：✅
- 输出摘要：L3 可自动化回归通过；本批 doc-standards 新增 `stage-exit-baseline.md` 已被镜像临时工程覆盖（fixture 12 文件、同步下行 ✓、派生 docs/00-09 不覆盖 ✓、check-derived-sync 接受 ✓）；全量自检 2112 项 / 0 失败。

## 人工项（按 `template-docs/maintainer/e2e-regression-checklist.md`）

- R4 场景引导路由：⏭️ 未跑——本批不触 `template-docs/scenario-guides.md` 与场景路由链路；路由入口 `ai/index.md` 改动为表内标注 + 脚注（已由全量自检覆盖格式与引用存在性）。
- R5 文档生成（review-inputs + generate-docs）：⏭️ 未跑——本批无 prompt / 生成链路改动；`stage-exit-baseline.md` 为规范基线参考件，不接入生成 prompt。
- R6 PowerShell fallback：⏭️ 全流程未跑（本机无法非破坏性隐藏 Git Bash——`Find-TemplateBash` 含 Program Files 绝对路径候选）。**补偿验证**：①`sync-template.ps1` / `check-derived-sync.ps1` Parser 解析通过；②Batch A 的 ps1 双版本通知函数 AST 抽取后三分支单元实测通过（匹配 → ✓ 确认行；漂移 → 漂移警告；无项目版本段 → 存量改写警告，含 957 行模板文件实测）；③ps1 包装器委派机制 fixture 实证（cwd = ps1 所在仓根、缺失 sh 时报错符合预期）。

## 问题与归因

- 测试过程曾出现「fixture 上命中『内容与母模板相同』分支」的假象——归因：测试姿势错误（从模板仓调用模板仓的 `sync-template.ps1`，包装器把 cwd 推到 `$PSScriptRoot/..` 即模板仓根，启发式套在模板仓自身文件上，哈希分支天然命中）。**非脚本缺陷**；该误跑同时反向实证了哈希分支的正确性。已确认模板仓工作区无残留污染（`git status` 仅既有 untracked 手册文件）。
- 改进观察：`sync-template.ps1` 的 `GIT_BASH` 提示语与实际查找逻辑不符（报错建议 `set GIT_BASH`，但 `Find-TemplateBash` 未读取该环境变量）——低优先级文案/行为不一致，登记待审，不阻塞本版。

## 结论

- [x] 可发布（自动化 R1–R3 + 全量自检通过；R4–R6 未跑项经补偿验证与触达面评估不构成阻断；人工项接受与否由维护者拍板）
- [ ] 需修复后重测
- [ ] 阻塞发布

## 后续动作

- 维护者确认本报告「可发布」结论后合入发版 PR。
- R4 / R5 / R6 全流程人工项建议随下一次触达场景 / 生成链路 / 同步脚本的 MINOR/MAJOR 发版补跑。
- `GIT_BASH` 查找不一致项待审（可作为后续 PATCH 候选，与 sync 脚本下次触达合并处理）。

# TextCleaner（参考样例）

> 这是一个**填好的参考样例**，演示“无数据库、但有 CLI 对外接口”项目如何使用本模板。仅供对照学习，不是可运行项目（代码省略，只展示文档）。

- **项目**：TextCleaner —— 本地文本清洗命令行工具
- **形态**：CLI 工具，无数据库、无 HTTP API
- **对照**：本目录结构对应模板根目录——`docs/00-05`、`docs/07`、`docs/08` 为填好的实例，`ai/project-rules.md` 为填好的项目专属规则
- **裁剪**：本项目无持久化存储，`docs/06-db-design.md` 省略；但有 CLI 对外接口，`docs/07-api-spec.md` 保留，用于描述命令 / 参数 / 退出码契约（不强求 RESTful）

阅读建议：先 `docs/00-scenario` → `02-srs`，再看 `03-05`、`07`、`08` 如何在无数据库的前提下保持闭环（07 描述 CLI 命令契约），最后看 `ai/project-rules.md` 的 §3 如何声明文档裁剪。

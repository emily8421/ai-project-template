# GitHub Issue #350: TEMPLATE-UPGRADE：pitfall 触发口径扩展——维护中发现的存量 AI 代码问题 + 「障眼法」模式登记

> Source URL: https://github.com/emily8421/ai-project-template/issues/350
> State: OPEN
> Labels: proposal from:LUMEN_demo_T2.1
> Author: emily8421
> Created: 2026-08-13T18:38:24Z
> Updated: 2026-08-13T18:38:24Z
> Mirrored at: 2026-08-17
> Mirror status: raw remote issue copy for local triage; GitHub issue remains source of comments and closure state.

## Raw Issue Body

# TEMPLATE-UPGRADE：pitfall 触发口径扩展——维护中发现的存量 AI 代码问题 + 「障眼法」模式登记

> 来源：LUMEN-DEMO（emily8421/LUMEN-DEMO）派生项目回流
> 提案日期：2026-08-13
> 目标文件：`ai/session-rules.md` §4.3（坑 / 问题观察日志）
> 状态：待提交模板维护者（跨仓 issue）

## 1. 动机

pitfall observation log（v1.61.1 引入）的触发描述是「任务收尾自检顺带判断**本次**是否产生坑观察」——会话视角，只覆盖「AI 当场踩的坑」。派生项目实践发现一个未覆盖的高价值场景：

**维护 / 治理任务中发现的存量 AI 代码问题**。AI 生成的代码常见「障眼法」形态：类型 / 接口 / 实现看似可行、功能演示通过，实际靠巧合运行（消费方碰巧只用到了存在的字段；测试碰巧没覆盖偏差路径）。这类问题在生成时不暴露，在后续维护（类型检查器引入、契约对齐、重构）中才被发现——此时已不在「本次会话产生」的口径内，容易不记录。

实证样本（派生项目，docs/09 审计记录背书）：

- 前端手写响应类型与后端实际契约多处失配（声明字段多于运行时、optionality 不一致、两种响应形状压平为 optional 字段当运行时哨兵），运行时均「碰巧没炸」——引入 openapi codegen 对齐时集中暴露（~17 域中 3 处需修正）。
- 引入 mypy 时抓到存量真实类型 bug ~20 个（current_space_id None 传播缺口 45 处）；引入 eslint 时抓到 `rules-of-hooks` 真 bug（tsc 未拦住）。
- 共性：生成时 AI「按想象中的形状」写类型 / 实现，内部自洽（类型检查通过、演示通过），与外部契约 / 运行时不一致。

## 2. 拟改（最小增量，不改机制结构）

`ai/session-rules.md` §4.3「触发与写入」补一条触发场景（去项目化表述）：

> - **存量代码维护触发**：维护、治理、重构或引入新检查器（类型检查 / lint / 契约对齐 / 测试）时，发现既有 AI 生成代码的缺陷、「看似可行实靠巧合运行」的障眼法实现、或类型 / 接口与实际契约的失配——即使问题非本次会话产生，也应记录单条（根因分类标「AI 引入」，现象注明发现场景），作为生成规范迭代的验证材料。

并在 §4.3 开头的机制定位句补一句（与 token-hotspot 管成本、pitfall 管质量的二分对齐）：

> pitfall 记录既覆盖「当场踩的坑」，也覆盖「维护中发现的存量 AI 代码问题」——后者是检验与完善代码生成规范的主要输入。

不新增目录、不改 rollup 阈值、不加 CI 门禁（与 §4.3 既有「无自检门禁」口径一致）。

## 3. 与既有规则关系（去重）

- §4.3 既有根因分类「AI 引入」已能承载此类记录，本提案只补**触发时机**的显式引导，不新增分类维度。
- `global-rules §2.1` L0-12「先契约后实现」是规范层的对应物；pitfall 记录为其提供验证证据，两者不重复。
- `implementation-lifecycle-rules §8`（代码事实反向同步）管「文档 vs 代码」冲突的处置流程；本提案管「AI 代码缺陷观察」的记录习惯，正交。

## 4. 版本影响与影响面

- 只改 `ai/session-rules.md` §4.3（+2 条 bullet / 1 句定位），同步清单内文件；属方法论行为引导，无代码 / 脚本 / 自检断言改动。
- 版本：MINOR bump（模板规则文件新增触发场景）；派生项目下行同步后自动生效。
- 兼容性：纯增量，不改变既有 pitfall 记录格式、路径分层与 rollup 阈值。

## 5. 验证方式

- `scripts/check-template.sh` 自检通过（无断言变化，预期绿）。
- 行为验证（维护者侧人工复核口径）：维护 / 治理任务收尾时，若发现存量 AI 代码缺陷，pitfall 自检应覆盖该场景——可在模板仓下次涉及「引入检查器 / 契约对齐」的维护任务中实际触发一次复核记录是否产生。

## 6. 备选方案与取舍

- **不扩展口径，靠「AI 引入」根因分类隐含覆盖**（现状）：实践中已被证明不稳定——派生项目 pitfall 目录 6 条全是「当场踩的」，存量问题零记录，直到用户主动提出才补。
- **把「生成代码类型必须源自机器可读契约」写成新 L0 规范条目**：样本仍集中在「前后端双写类型」这一形态，通用性待更多项目验证；建议先经 pitfall summary 积累证据，再另起提案（本提案不动 L0）。

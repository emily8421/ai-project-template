# Token Hotspot 记录：issue #221 派生版本机制阶段 B 实现 + 提交 + PR

> 日期：2026-07-16
> 任务类型：模板维护（阶段 B 实现：check-derived-sync 双脚本非阻断检测段 + post-sync-cleanup 引导步 + check-template 防回归断言 + v1.54.0 + PR #224）
> 触发条件：`ai/session-rules.md` §4.1（从快速续接进入实现后完整读取规则回退包 + 大文件 `check-template.sh` + 多次长输出命令）

## 热点工作

- 完整规则回退包读取：`rules-core` / `global-rules` / `document-lifecycle-rules` / `implementation-lifecycle-rules` / `project-rules` / `session-rules`（模板维护 + 自检脚本变更按 `rules-core` §3 要求完整回退包；阶段 A 已读，本轮重读）。
- 大文件：`scripts/check-template.sh`（1614 行，分段读 + grep 定位断言区，找阶段 B 防回归断言插入点 §954 并避开阶段 A 已有断言 §319 / §1170 / §1172）、`check-derived-sync.sh`（186 行）/ `.ps1`（375 行）全文（定位非阻断段插入点 + LINEAGE_ROLE 复用 + `set -u` 兜底）、`15-post-sync-cleanup.md`（169 行，定位步骤 5 插入点）。
- `check-template.sh` 跑 2 次（实现后 + v1.54.0 bump 后），均 EXIT=0；重定向 + grep 摘要（版本断言 / 新断言 / ✗ 失败项），未全刷入。
- 派生项目手动验证 3 次（已启用双信号 pass；root commit 假阴性排查 `git diff-tree` 对根提交不输出变更；干净已启用 EXIT=0），临时 git 仓 + 未跟踪文件构造。
- `e2e-sync-check.sh`（59 行）+ 提案 6 处 Edit 更新（§3.1 / §4 / §5.1 / §6）。

## 热点判断

- **主要成本**：`check-template.sh`（1614 行，虽分段读 + grep 仍是大头）。
- **次要**：规则回退包重读（模板维护任务再次触发完整回退包）；check-derived-sync 双脚本全文（必要——精确插入检测段 + `set -u` / LINEAGE_ROLE 兜底）。
- **总量级**：medium。

## 质量影响

- H-001（same-session rule-reuse）部分生效：阶段 A 已读规则，本轮同会话复用上下文；但模板维护 + 自检脚本变更按 `rules-core` §3 仍要求完整回退包，故重读。
- H-003（验证证据摘要）生效：check-template / e2e 成功输出只留 EXIT + 摘要 + ✗ 检查。
- 关键合规：C-004 主 / 辅双信号基线落定（修正 proposal 原表「对照 TEMPLATE-BASE Current synced template version」判断）；辅信号用「项目版本管理」子串匹配规避 C-002 节号漂移；domain 豁免复用既有 LINEAGE_ROLE；检测段全非阻断 → MINOR 向后兼容。
- 复用既有模式：检测段仿 check-derived-sync 现有「根 README 模板版本号一致性（非阻断）」段，降低新代码风险。

## 优化建议

- **check-template.sh 1614 行可拆分**：自检断言已超 1600 行，可考虑按主题（入口 / 文档骨架 / 版本治理 / 脚本 / 同步清单 / check-derived-sync 区）拆成 sourced 子文件，降低每次定位断言区的读取成本。当前靠 grep 定位插入点可行，但行数持续增长。
- **check-derived-sync 检测段双脚本镜像**：`.sh` 与 `.ps1` fallback 的检测逻辑高度镜像（.ps1 为英文复刻）；长期可考虑单一真相源 + 生成，但当前双镜像是既有约定（.ps1 fallback 只做结构 + 关键内容断言）。
- **C-002（节号硬编码）推广**：本轮辅信号特意用「项目版本管理」子串而非 §2.8 节号，规避节号漂移；可推广到其他节号类断言，降低 §2.x 重排时的断言失效风险。

## 关联

- issue #221；PR #224（阶段 B，待评审）；分支 `change/derived-version-stage-b`；main = v1.53.0，分支 bump v1.54.0。
- 阶段 A hotspot：`ai-records/token-hotspots/2026-07-16-issue-221-stage-a.md`（已 commit 1 `590a7f8`）。
- 提案：`_proposals/TEMPLATE-UPGRADE-derived-version-mechanism.md` §3.1 / §5.1（阶段 B 实现 + 验证记录）。
- 机制：`_proposals/TEMPLATE-UPGRADE-token-hotspot-records.md`（H-001 / H-003 落地）。

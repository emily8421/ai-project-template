# L3 端到端回归报告

> Template-only notice: 本文件由 ai-project-template 模板维护者维护，仅存在于模板仓（v1.66.0 起不进入下行同步清单）；历史版本曾下行，派生项目中的残留可安全删除。

> 模板发布前 L3 回归记录（v1.75.0 MINOR 发版门）。

## 基本信息

- 待发布版本：v1.75.0（三层布局模式定型：`TEMPLATE-UPGRADE-domain-layout` 提案 Batch A + B(R0+)，C-001~C-005 全按 AI 建议裁决）
- 测试日期：2026-09-10
- 测试人：AI 维护者会话（结果待维护者确认）
- 环境（OS / Git Bash / PowerShell / `gh`）：Windows 11 / Git Bash（MSYS）+ PowerShell 5.1 / gh（emily8421）
- 测试派生项目：e2e-sync-check 内建临时 fixture（2 假派生 + 1 非派生）；domain 保护另用临时伪领域仓做函数级验证（已清理）

## 自动化项（`bash scripts/e2e-sync-check.sh`）

- check-template（含同步链路 + doc-standards 镜像 + 新项目烟测 + 同步清单一致性）：✅
- sync-all-derived 批量烟测：✅
- 输出摘要：L3 可自动化回归通过（exit 0）；全量自检 2114 项 / 0 失败（与 #459 后基线 2114 一致；本轮 3 条断言为 pattern 内容更新、不增删，见「问题与归因」）。

## 人工项触达面评估（按 `template-docs/maintainer/e2e-regression-checklist.md`）

本批改动面：规则 / 方法论文档 8 件（global-rules、domain-templates、domain-rules 基线、剧本骨架、lab 命令 + prompt、scenario-guides A20 步骤 5 一行、inheritance 提案）+ 脚本 3 件（check-derived-sync.sh/.ps1 保护清单各 1 行、check-template.sh 断言 pattern 3 行）+ 版本三件套（VERSION / CHANGELOG / CHANGELOG-PLAIN）+ 新提案 1 件 + 本报告。

- R4 场景引导路由：⏭️ 未跑——不触场景路由结构（A 索引 / 判定链路未改）；`scenario-guides.md` 仅 A20 步骤 5 单元格路径口径更新（`domain/scenarios.md`），A20 本身属领域模板创建场景，本发版不创建领域模板。
- R5 文档生成（review-inputs + generate-docs）：⏭️ 未跑——不触生成 prompt 链路；改动仅 prompt 23 的头部标记行与资产表路径，不新增必读入口。
- R6 PowerShell fallback：✅ 补偿验证已跑——本轮改 `check-derived-sync.ps1`（保护清单 +1 行）：`Test-ProtectedProjectFile` 函数级 7 例测试全过（`domain/*` 3 例保护、`ai/domain-rules.md` 与 `docs/0[0-9]-*` 回归例、2 反例不误保护）+ 全脚本 Parser 解析 0 错误；Bash 侧 `is_protected_project_file` 同 7 例全过。`sync-template.ps1` 未改动。

## 问题与归因

- 首轮全量自检 3 失败 / 2111 通过：三条断言（check-template.sh :1864 / :1867 / :1875）仍断言旧剧本路径字面 `domain-derived-scenarios\.md`，与本批「剧本路径收敛为 `domain/scenarios.md`」的行为变更冲突。处置：断言 pattern 同步更新为新路径（内容更新、不增删，计数 2114 不变），复跑 e2e-sync-check 全过。属行为变更的配套断言更新（#459 同类先例），非断言缺失。
- 临时伪领域仓冒烟：`check-derived-sync.sh` 在缺 `template-sync.json` 的 fixture 上按预期报缺清单（环境过简非脚本缺陷），故保护验证改用函数级精确测试（结论见 R6），临时目录已清理。

## 结论

- [x] 可发布（自动化 R1–R3 + 全量自检通过；R4/R5 经触达面评估豁免留痕；R6 补偿验证通过；人工项接受与否由维护者拍板）
- [ ] 需修复后重测
- [ ] 阻塞发布

## 后续动作

- 维护者确认本报告「可发布」结论后合入发版 PR（v1.75.0）。
- 合并后：v1.75.0 归入派生同步窗口（与 v1.72.x–v1.74.0 待同步各仓合并处理；领域路线仓 agent-system-template 同步时核对剧本路径口径与保护清单更新）。
- Batch C（agent-system-template 按 §5.1 迁移领域内容入 `domain/`）：L2 仓独立会话执行；Batch D（Web 类 L2 建仓）按候选观察转化时套用。

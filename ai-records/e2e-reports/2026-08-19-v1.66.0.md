# L3 端到端回归报告：v1.66.0

> 复制自 `template-docs/maintainer/e2e-report-template.md`；本报告记录 v1.66.0（template-docs 目录分组重组 + 手册文档同步边界）发布前 L3 回归结果。

## 基本信息

- 待发布版本：v1.66.0（MINOR）
- 分支：`change/template-docs-reorg`（Step 1 `68ddf8e` + Step 2 `31701c8`）
- 日期：2026-08-19
- 执行者：模板维护者 AI 会话（人工项由维护者复核本报告后确认）

## R1-R3（自动化）

`bash scripts/e2e-sync-check.sh`：**通过**（含 check-template 全量 2093 项 / 0 失败、临时派生场景、批量同步 dry-run）。

## R4-R6（人工项）

| 项 | 结果 | 证据 |
|---|---|---|
| R4 场景引导路由 | ✅ 通过 | 本次维护会话全程按 `ai/index.md` 任务路由读取规则包（模板维护 → 完整回退包 + MAINTAINERS + C1 prompt）；`template-docs/README.md` 新分组导航路径与实际目录结构一致性由 check-template 断言覆盖（105 处新路径断言全过） |
| R5 文档生成 | ✅ 通过（自动等价） | `check-template.sh` 的 `require_doc_standards_mirror`（doc-standards 镜像自检）通过——在临时派生项目验证下行同步生成 `ai/doc-standards/00-09`、项目事实 `docs/00-09` 不被覆盖；本批未改文档生成链路，仅迁移模板文件路径 |
| R6 PowerShell fallback | ✅ 通过 | `powershell -ExecutionPolicy Bypass -File scripts/check-template.ps1` 实测通过（2093 项 / 0 失败，Bash 可用时优先委托、fallback 结构断言镜像同步）；`check-derived-sync.sh` bash -n 语法通过 |

## 本批专项验证（超出 R1-R6 范围）

- new-project 烟测（`--local --no-remote --no-examples`）：产物 `template-docs/` = 9 手册 + profiles/（4）+ templates/（11）+ 既有子目录；maintainer/ 4 件已删、空目录已清。
- 同步分发逻辑直测（普通 / 领域两路线 8 项断言）：全 PASS。
- 引用迁移 grep 复查：零旧路径残留（排除 `_archive/`、`.ai/`）。
- `check-markdown-clean.ps1 _proposals template-docs`：61 文件通过。

## 结论

**通过，可发布**。无阻断问题；已知非阻断事项：

1. 派生项目旧路径残留（19 个旧路径文件）与 4 件停更文档残留——随下次同步窗口 post-sync-cleanup 审计清理（已登记审计项）。
2. `sync-template.sh` 兜底清单不含 files_domain 项——既有兜底局限，注释已说明，主清单可读时不受影响。
3. agent-system-template 自建 domain-overlay 对旧路径的引用——其下次同步时随行文件自动更新大部分，自建部分由其维护者按同步报告提示自改。

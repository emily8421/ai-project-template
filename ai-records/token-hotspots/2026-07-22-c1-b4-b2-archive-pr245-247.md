# Token Hotspot 记录：C1 B4（#232 落地）+ B2（sync 体验）+ #232 镜像归档

> 日期：2026-07-22
> 任务类型：C1 triage 续作（B4 web 主文件膨胀 #232 落地 + B3 记录回写 + B2 sync 体验文档 / prompt + #232 镜像归档），3 个 PR/CI 闭环（#245 / #246 / #247）
> 触发条件：`ai/session-rules.md` §4.1（从快速续接进入模板维护 + 多轮 PR/CI 闭环；读 batch plan / 多份提案 / global-rules / web-fullstack-profile / 12-sync-template / git-guide + 多次 git/gh + CI 轮询）
> 汇总状态：未汇总（待纳入 SUMMARY.md 更新）

## 热点工作

- **C1 triage 续作**：基于 `_proposals/TEMPLATE-UPGRADE-C1-triage-batch-plan.md` 推进 B3 记录回写 + B4 + B2。
- **B3 记录回写**（随 #245）：发现 PR #244 = B3 P1（template-check-maintainability 提案 P1），但 batch plan §6 未回写；读 template-check-maintainability 提案确认 P1 覆盖（3/4 项）；更新 batch plan §3/§6 + 提案状态 + token-hotspot-records H-005 / 失败诊断候选指向。
- **B4（#232）triage 重大修正**：读 web-fullstack-profile §5 发现 #232 §1 描述过时（阈值 300 行已于 capability batches 落地）；读 global-rules §5 确认已引用 Profile。把 B4 从设想的 MINOR 大任务收敛为 patch（§5.1 主文件职责边界 + 业务下沉）。
- **B4 落地**：web-fullstack-profile §5.1 + 提案 + VERSION v1.56.3 + CHANGELOG；PR #245 squash 合并；#232 评论说明阈值已落地 + §5.1 增量 + 关闭。
- **B2（sync 体验）**：读 windows-sync-output-noise 提案 + 12-sync-template A13 SOP（24 步全文）；按 Release strategy「优先文档 / prompt」只做 git-guide §5.8 + 12-sync 第 8/9 步（脚本降噪留后续）；PR #246。
- **#232 镜像归档**：确认 `_archive/proposals/_remote-issues/` 先例（#238 / #234 / #235 等 29 份）；Move + 首次提交（镜像此前一直未跟踪）；PR #247（none impact）。

## 热点判断

- **主要成本**：① triage 读取（batch plan + template-check-maintainability + token-hotspot-records + windows-sync-output-noise + issue-232 + global-rules §5 + web-fullstack-profile + 12-sync-template 全文）；② 多轮 PR/CI 闭环（#245 / #246 / #247 各：分支 + commit + push + PR + CI 轮询 + 合并 + 分支清理）；③ CI pending 二次轮询（#247 第一次 sleep 15s 仍 pending，第二次 sleep 20s 才 pass）。
- **输出控制**：规则按 §3.2 同会话复用（B4 / B2 / #232 同属 C1 维护任务链，未重读门禁规则）；git-guide §5.7 等已有内容不重读（用既有上下文）；CI 失败日志最小化（只看 pass / exit）。
- **远端风险点**：3 次 push + 3 次 PR + 3 次 squash 合并 + #232 评论 / 关闭，均单步确认（用户多次 `AskUserQuestion` 授权）；直连 GitHub 全程成功（未启用代理 7897）。
- **不确定性控制**：#232 描述过时严格用 web-fullstack-profile §5 + global-rules §5 佐证（非臆断）；B3 = PR#244 用 template-check-maintainability 提案 P1 表对照确认；B2 脚本降噪明确留后续（避免过早脚本化）。
- **总量级**：high（triage 续作 + 3 轮 PR/CI 闭环 + 跨提案协调 + #232 远端闭环）。

## 质量影响

- **正向**：B4 triage 修正避免 MINOR 大改（收敛为 patch）；B2 按 Release strategy 分阶段（文档先于脚本）；#232 完整闭环（落地 + 评论 + 关闭 + 镜像归档）；B3 记录回写保持 batch plan 一致。
- **风险控制**：每轮独立分支 + PR（主题分离）；CI 全 pass；提案去项目化；镜像 raw 保持（State 字段不改，#238 先例）。
- **triage 方法论产出**：①「提案描述过时」是常见模式（#232 §1 vs 实际 §5）——triage 必须对照现状而非提案自述；② Release strategy「文档 / prompt 优先，脚本单独 PR」降低单 PR scope；③ 同会话 §3.2 规则复用显著降本（B4 / B2 / #232 未重读门禁）。

## 优化建议

- **CI 轮询**：#247 第一次 sleep 15s 仍 pending——template-check 实际 ~11–17s，sleep 应 ≥20s 或用 Monitor 等单次完成通知，避免二次轮询往返。
- **triage 对照**：C1 batch plan 的「候选」状态可能与实际进度漂移（B3 标候选但实际 PR#244 已落地）——合并后应及时回写 batch plan §6（本轮已修）。
- **hotspot 记录节奏**：连续多轮 PR 闭环（B4 / B2 / #232）未每轮即时记录，累积补记——可在每轮 PR 合并后即写单条记录（而非累积），降低事后回忆成本。

# 02 需求规格说明（SRS）

- REQ-1：系统必须提供一个命令行入口，支持 `--input` 和 `--output` 两个参数。
- REQ-2：系统必须读取输入文件内容，并执行基础清洗：去掉每行首尾空白、合并连续空行为单个空行。
- REQ-3：系统必须将清洗结果写入输出文件。
- REQ-4：当输入文件不存在、输入输出路径缺失或读取失败时，系统必须返回非零退出码并输出可理解的错误信息。
